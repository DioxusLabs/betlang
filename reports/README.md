# Generated Reports

This directory holds generated analysis artifacts that are useful for reviewing
model behavior but are not part of the published crate package.

Evaluation metrics for the shipped 2-class model (accuracy and per-class
recall) are printed by `scripts/train_prompt_student.py` at export time; see
`scripts/TRAINING.md`.
